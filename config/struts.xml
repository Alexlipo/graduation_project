<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
	"-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
	"http://struts.apache.org/dtds/struts-2.3.dtd">

<struts>
	<!-- 禁用动态方法访问 -->
	<constant name="struts.enable.DynamicMethodInvocation" value="false" />
	<!-- 配置成开发模式 -->
	<constant name="struts.devMode" value="true" />
	<!-- 配置拓展名为action -->
	<constant name="struts.action.extention" value="action" />
	<!-- 把主题配置成simple -->
	<constant name="struts.ui.theme" value="simple" />

	<!-- 配置全局异常 结果映射-->
	<package name="base-default" extends="struts-default">
		<!-- 全局返回结果 -->
		<global-results>
			<result name="sysError">/WEB-INF/jsp/error.jsp</result>
			<result name="input">/WEB-INF/jsp/error.jsp</result>
		</global-results>
		<global-exception-mappings>
			<exception-mapping result="sysError" exception="com.lyg.core.exception.SysException"></exception-mapping>
			<exception-mapping result="input" exception="java.lang.Exception"></exception-mapping>
		</global-exception-mappings>
	</package>
	
	<!-- 包含test的struts配置文件 -->
	<include file="com/lyg/test/conf/test-struts.xml"></include>
	<!-- 包含jxc/conf下所有的struts配置文件 -->
	<include file="com/lyg/jxc/conf/*-struts.xml"></include>
	<!-- 包含登录的配置文件 -->
	<include file="com/lyg/login/conf/login-struts.xml"></include>
	
</struts>
